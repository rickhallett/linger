# Website source of truth

Read README.md before changing the website. The canonical design is Osaka Jade in `linger/`, selected by the operator on 13 September 2026. Edit that site for normal website work; use `astro.linger.config.mjs` and `pnpm build`.

Earlier design variants are archived locally under `../outputs/archive/website-designs-2026-09-13/`. They are historical references, not active alternatives or instructions. Do not restore their routes, gallery, styles or copy unless the operator explicitly requests it. The inherited `src/`, `public/` and WASM app are also not the canonical Linger marketing site.

Keep `/` and `/guide/` consistent, preserve fictional demo data and upstream attribution, and do not add eyebrow elements. Website promotion does not authorize pushing, publishing or releasing the terminal application.
